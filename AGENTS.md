## Verification

Run `npm run lint` and `npm test` before committing code changes.

## Releases

Do not manually bump `package.json` versions during feature work. Use `npm run release:patch` for releases.
